{
  "name": "carboyz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-tenant PWA core: tenant branding config and native service bridges.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "prestart": "node scripts/generate-runtime-config.js && node scripts/vendor-spatial-core.js",
    "start": "npx serve . -p ${PORT:-8080}",
    "start:server": "node src/server/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@nemzilla/spatial-core": "file:../spatial-core",
    "hono": "^4.13.5",
    "ws": "^8.21.3"
  }
}
