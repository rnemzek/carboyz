# HYDRATE ACTIVE EXECUTION CANVAS

## Completed: UOW-07 — Structured LLM Query Parsing & Local Discovery Sync

- [x] **Task 7.1:** Add an LLM parse handler (`parseChatQueryWithLLM`) to `src/adapters/chatFilterAdapter.js` that extracts `{maxPrice, maxMileage, minYear, bodyStyle, conditionPreference, intentSummary}` from natural-language queries via the Anthropic Messages API (dependency-injectable `fetchImpl`/`apiKey` for testability), falling back gracefully to the existing regex parser (`resolveChatQuery`) whenever no API key is configured, the request fails/times out, or the response fails validation.
- [x] **Task 7.2:** Wire the structured query into the local discovery pipeline (`src/ui/MapView.js`): the chat bar now resolves via `resolveChatQuery`, filters the candidate pool down to vehicles at dealers within the existing 25-mile H3 nearby-cell set, ranks them through `rankTopMatches` (which runs `evaluateMarketComps()` per candidate), and renders the Top-5 in `ChatDiscovery`'s results drawer alongside a natural-language `intentSummary` line.
- [x] **Task 7.3:** Confirmed the map/card sync (`focusDealer` → `map.flyTo` + `showDrawer({ highlightVehicleId })`) was already correctly wired from a prior pass; no changes needed.
- [x] **Task 7.4:** Extended `filterVehiclesByQuery` to honor `conditionPreference` against `deriveVehicleCondition()`. Added unit tests for the LLM handler (success, invalid bodyStyle, network failure, non-ok response, unparseable response, no-API-key short-circuit), `resolveChatQuery` fallback behavior, and the new `conditionPreference` filter.
- [x] **Task 7.5:** Ran `npm test` (167/167 pass, chatFilterAdapter.js at 99.20% line / 87.07% branch coverage) and a headless Playwright pass driving the live app end-to-end: load `?brand=carboyz` → Map tab → submit "SUV under $35k" → results drawer renders 2 cards with correct verdict badges (Fair Market/Overpriced) and intent summary → clicking a card flies the map to CarBoyZ Motors HQ and highlights the matching vehicle in the dealer drawer. Zero console errors.

## Completed: UOW-08 — Location Overlay: GPS Locate & ZIP/City Search

> Motivated by the UOW-07 journal note: `FALLBACK_LOCATION` in `src/ui/MapView.js` is hardcoded to Orange County, CA (~2,500 mi from the seeded CarBoyZ HQ / `SEED_ANCHOR`), so an un-geolocated session finds zero local matches. This UOW gives buyers a friendlier default plus a manual override.

- [x] **Task 8.1:** Add `src/adapters/locationAdapter.js`: a small offline `GeocoderResolver`-shaped adapter (matches `@nemzilla/spatial-core`'s pluggable resolver interface) backed by a static ZIP/city → `{ lat, lng, label }` lookup table (seeded with Leland NC 28451 plus the existing demo cities referenced in `App.js`'s `TENANT_PRESETS`/`seedInventory.js`). Export `resolveLocationQuery(text)` (ZIP or "City, ST" → point or `null` if unrecognized) and `describeCoordinates(lat, lng)` (nearest known label, for captioning a GPS fix). No network calls — deterministic and unit-testable, consistent with `chatFilterAdapter`'s injectable-fallback pattern.
- [x] **Task 8.2:** Update `FALLBACK_LOCATION` in `src/ui/MapView.js` from Orange County, CA (`{ lat: 33.6846, lng: -117.8265 }`) to Leland, NC (`{ lat: 34.2388, lng: -78.0145 }`). Scope check: this is the *no-geolocation default* only — leave `SEED_ANCHOR` (`src/utils/seedInventory.js`) and the `TENANT_PRESETS` dealer coordinates in `App.js` untouched; they're seeded domain data, not the fallback-location concern.
- [x] **Task 8.3:** Add a top Location Overlay bar to `MapView.js`'s map section: `📍 {label} ({radiusMiles} mi)`, derived from `userLocation`/`FALLBACK_LOCATION` via `describeCoordinates` and the existing `NEARBY_RADIUS_KM`. Tapping the bar opens a lightweight modal (reuse the `.modal-overlay`/`.modal` pattern already in `styles.css`) with a text input ("Enter ZIP Code or City..."). On submit, resolve via `locationAdapter`, `map.flyTo({ center: [lng, lat], zoom: DEFAULT_ZOOM })`, recompute `nearbyDealers` via the existing `filterDealersNearby` (spatial-core `getNearbyCells`), re-render the layer, and update the overlay label. Show an inline error for unrecognized input rather than closing the modal.
- [x] **Task 8.4:** Add a floating "Locate Me" `🎯` button pinned bottom-right of the map canvas (≥44px touch target, mobile-first). Tap re-runs `requestUserLocation()` → `applyUserLocation()` (both already implemented), then updates the overlay label via `describeCoordinates`. Show a brief loading/disabled state while `getCurrentPosition` is in flight; on denial/timeout, leave the current location in place (no crash, no silent no-op — surface a brief inline message).
- [x] **Task 8.5:** Style the new elements in `src/ui/styles.css` (`.map-location-bar`, `.map-locate-btn`, search modal) following the existing mobile-first/`--spacing`/`--radius` conventions and the `.map-drawer`/`.modal` z-index stacking already in place.
- [x] **Task 8.6:** Unit tests for `locationAdapter.js` in `tests/locationAdapter.test.js` (ZIP hit, city hit, unrecognized input, `describeCoordinates` nearest-match) at ≥80% line/branch coverage. `MapView.js` itself has no existing unit test target (requires a DOM + `maplibregl` global) — keep the new resolution/labeling logic in the testable adapter rather than inline in `MapView.js`, matching how `chatFilterAdapter` is factored out of `MapView.js` today.
- [x] **Task 8.7:** Run `npm test` (must stay green, new module ≥80% coverage) and a headless Playwright pass against `?brand=carboyz` → Map tab: confirm the overlay reads "Leland, NC" by default, click `🎯` with a mocked geolocation grant and confirm the map recenters + overlay updates, then open the search modal, submit a ZIP/city, and confirm `map.flyTo` fires and dealer pins re-render for the new location. Log results to `docs/journals/dev-journal.md`, check off tasks here, and update `ROADMAP.md`.

# All UOWs are complete!
Run 'hydrate prompt' when ready for next task.
